// Code generated by specgen. DO NOT EDIT.
// Source: specs/service_config.yaml
// Command: service_config
// Reference:

package parsers

import (
	"encoding/json"
	"strings"
	"testing"
)

// TestSpecServiceConfigRTXSyntax validates that RTX commands in spec are well-formed
func TestSpecServiceConfigRTXSyntax(t *testing.T) {
	// This test validates that all RTX command strings in the spec are well-formed
	// and follow expected patterns

	testCases := []struct {
		name       string
		rtxCommand string
		parseOnly  bool
		buildOnly  bool
	}{
		{
			name:       "httpd_host_any",
			rtxCommand: `httpd host any`,
			parseOnly:  false,
			buildOnly:  false,
		},
		{
			name:       "httpd_host_lan1",
			rtxCommand: `httpd host lan1`,
			parseOnly:  false,
			buildOnly:  false,
		},
		{
			name:       "httpd_host_lan2",
			rtxCommand: `httpd host lan2`,
			parseOnly:  false,
			buildOnly:  false,
		},
		{
			name:       "httpd_proxy_access_on",
			rtxCommand: `httpd proxy-access l2ms permit on`,
			parseOnly:  false,
			buildOnly:  false,
		},
		{
			name:       "httpd_proxy_access_off",
			rtxCommand: `httpd proxy-access l2ms permit off`,
			parseOnly:  false,
			buildOnly:  false,
		},
		{
			name:       "sshd_service_on",
			rtxCommand: `sshd service on`,
			parseOnly:  false,
			buildOnly:  false,
		},
		{
			name:       "sshd_service_off",
			rtxCommand: `sshd service off`,
			parseOnly:  false,
			buildOnly:  false,
		},
		{
			name:       "sshd_host_single",
			rtxCommand: `sshd host lan1`,
			parseOnly:  false,
			buildOnly:  false,
		},
		{
			name:       "sshd_host_multiple",
			rtxCommand: `sshd host lan1 lan2`,
			parseOnly:  false,
			buildOnly:  false,
		},
		{
			name:       "sshd_host_any",
			rtxCommand: `sshd host any`,
			parseOnly:  false,
			buildOnly:  false,
		},
		{
			name:       "sshd_auth_password",
			rtxCommand: `sshd auth method password`,
			parseOnly:  false,
			buildOnly:  false,
		},
		{
			name:       "sshd_auth_publickey",
			rtxCommand: `sshd auth method publickey`,
			parseOnly:  false,
			buildOnly:  false,
		},
		{
			name:       "sshd_host_key_generate",
			rtxCommand: `sshd host key generate`,
			parseOnly:  false,
			buildOnly:  true,
		},
		{
			name:       "sftpd_host_single",
			rtxCommand: `sftpd host lan1`,
			parseOnly:  false,
			buildOnly:  false,
		},
		{
			name:       "sftpd_host_multiple",
			rtxCommand: `sftpd host lan1 lan2`,
			parseOnly:  false,
			buildOnly:  false,
		},
		{
			name:       "delete_httpd_host",
			rtxCommand: `no httpd host`,
			parseOnly:  false,
			buildOnly:  true,
		},
		{
			name:       "delete_sshd_service",
			rtxCommand: `no sshd service`,
			parseOnly:  false,
			buildOnly:  true,
		},
		{
			name:       "delete_sshd_host",
			rtxCommand: `no sshd host`,
			parseOnly:  false,
			buildOnly:  true,
		},
		{
			name:       "delete_sftpd_host",
			rtxCommand: `no sftpd host`,
			parseOnly:  false,
			buildOnly:  true,
		},
	}

	for _, tc := range testCases {
		t.Run(tc.name, func(t *testing.T) {
			// Validate RTX command is not empty
			if strings.TrimSpace(tc.rtxCommand) == "" {
				t.Errorf("RTX command should not be empty")
			}

			// Validate command doesn't have trailing/leading whitespace issues
			trimmed := strings.TrimSpace(tc.rtxCommand)
			if tc.rtxCommand != trimmed && !strings.Contains(tc.rtxCommand, "\n") {
				t.Errorf("RTX command has unexpected whitespace: %q", tc.rtxCommand)
			}

			// Log the command for visibility
			if !tc.buildOnly {
				t.Logf("Parse test: %s", tc.rtxCommand)
			}
			if !tc.parseOnly {
				t.Logf("Build test: %s", tc.rtxCommand)
			}
		})
	}
}

// TestSpecServiceConfigSyntaxCoverage documents the syntax patterns covered by this spec
func TestSpecServiceConfigSyntaxCoverage(t *testing.T) {
	// This test documents all syntax patterns defined in the spec
	// Each pattern should have corresponding implementation in the parser

	syntaxPatterns := []struct {
		name        string
		rtxCommand  string
		parseOnly   bool
		buildOnly   bool
		description string
	}{
		{
			name:        "httpd_host_any",
			rtxCommand:  `httpd host any`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "httpd_host_lan1",
			rtxCommand:  `httpd host lan1`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "httpd_host_lan2",
			rtxCommand:  `httpd host lan2`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "httpd_proxy_access_on",
			rtxCommand:  `httpd proxy-access l2ms permit on`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "httpd_proxy_access_off",
			rtxCommand:  `httpd proxy-access l2ms permit off`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "sshd_service_on",
			rtxCommand:  `sshd service on`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "sshd_service_off",
			rtxCommand:  `sshd service off`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "sshd_host_single",
			rtxCommand:  `sshd host lan1`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "sshd_host_multiple",
			rtxCommand:  `sshd host lan1 lan2`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "sshd_host_any",
			rtxCommand:  `sshd host any`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "sshd_auth_password",
			rtxCommand:  `sshd auth method password`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "sshd_auth_publickey",
			rtxCommand:  `sshd auth method publickey`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "sshd_host_key_generate",
			rtxCommand:  `sshd host key generate`,
			parseOnly:   false,
			buildOnly:   true,
			description: "",
		},
		{
			name:        "sftpd_host_single",
			rtxCommand:  `sftpd host lan1`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "sftpd_host_multiple",
			rtxCommand:  `sftpd host lan1 lan2`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "delete_httpd_host",
			rtxCommand:  `no httpd host`,
			parseOnly:   false,
			buildOnly:   true,
			description: "",
		},
		{
			name:        "delete_sshd_service",
			rtxCommand:  `no sshd service`,
			parseOnly:   false,
			buildOnly:   true,
			description: "",
		},
		{
			name:        "delete_sshd_host",
			rtxCommand:  `no sshd host`,
			parseOnly:   false,
			buildOnly:   true,
			description: "",
		},
		{
			name:        "delete_sftpd_host",
			rtxCommand:  `no sftpd host`,
			parseOnly:   false,
			buildOnly:   true,
			description: "",
		},
	}

	t.Logf("Total syntax patterns: %d", len(syntaxPatterns))

	parseOnlyCount := 0
	buildOnlyCount := 0
	bidirectionalCount := 0

	for _, p := range syntaxPatterns {
		if p.parseOnly {
			parseOnlyCount++
		} else if p.buildOnly {
			buildOnlyCount++
		} else {
			bidirectionalCount++
		}
	}

	t.Logf("Parse-only patterns: %d", parseOnlyCount)
	t.Logf("Build-only patterns: %d", buildOnlyCount)
	t.Logf("Bidirectional patterns: %d", bidirectionalCount)
}

// TestSpecServiceConfigBoundaryCoverage documents the boundary values covered by this spec
func TestSpecServiceConfigBoundaryCoverage(t *testing.T) {
	// This test documents all boundary value tests defined in the spec
	boundaryParams := []string{
		"auth_method",
		"host",
	}

	t.Logf("Parameters with boundary tests: %d", len(boundaryParams))
	for _, param := range boundaryParams {
		t.Logf("  - %s", param)
	}
}

// TestSpecServiceConfigRTXTerraformMapping validates RTX command to Terraform value mappings
func TestSpecServiceConfigRTXTerraformMapping(t *testing.T) {
	// This test validates that each RTX command has a corresponding expected Terraform value
	// and vice versa. It ensures the spec file correctly documents the bidirectional mapping.

	testCases := []struct {
		name          string
		rtxCommand    string
		terraformJSON string
		parseOnly     bool
		buildOnly     bool
	}{
		{
			name:          "httpd_host_any",
			rtxCommand:    `httpd host any`,
			terraformJSON: `{"httpd":{"host":"any"}}`,
			parseOnly:     false,
			buildOnly:     false,
		},
		{
			name:          "httpd_host_lan1",
			rtxCommand:    `httpd host lan1`,
			terraformJSON: `{"httpd":{"host":"lan1"}}`,
			parseOnly:     false,
			buildOnly:     false,
		},
		{
			name:          "httpd_host_lan2",
			rtxCommand:    `httpd host lan2`,
			terraformJSON: `{"httpd":{"host":"lan2"}}`,
			parseOnly:     false,
			buildOnly:     false,
		},
		{
			name:          "httpd_proxy_access_on",
			rtxCommand:    `httpd proxy-access l2ms permit on`,
			terraformJSON: `{"httpd":{"proxy_access":true}}`,
			parseOnly:     false,
			buildOnly:     false,
		},
		{
			name:          "httpd_proxy_access_off",
			rtxCommand:    `httpd proxy-access l2ms permit off`,
			terraformJSON: `{"httpd":{"proxy_access":false}}`,
			parseOnly:     false,
			buildOnly:     false,
		},
		{
			name:          "sshd_service_on",
			rtxCommand:    `sshd service on`,
			terraformJSON: `{"sshd":{"enabled":true}}`,
			parseOnly:     false,
			buildOnly:     false,
		},
		{
			name:          "sshd_service_off",
			rtxCommand:    `sshd service off`,
			terraformJSON: `{"sshd":{"enabled":false}}`,
			parseOnly:     false,
			buildOnly:     false,
		},
		{
			name:          "sshd_host_single",
			rtxCommand:    `sshd host lan1`,
			terraformJSON: `{"sshd":{"hosts":["lan1"]}}`,
			parseOnly:     false,
			buildOnly:     false,
		},
		{
			name:          "sshd_host_multiple",
			rtxCommand:    `sshd host lan1 lan2`,
			terraformJSON: `{"sshd":{"hosts":["lan1","lan2"]}}`,
			parseOnly:     false,
			buildOnly:     false,
		},
		{
			name:          "sshd_host_any",
			rtxCommand:    `sshd host any`,
			terraformJSON: `{"sshd":{"hosts":["any"]}}`,
			parseOnly:     false,
			buildOnly:     false,
		},
		{
			name:          "sshd_auth_password",
			rtxCommand:    `sshd auth method password`,
			terraformJSON: `{"sshd":{"auth_method":"password"}}`,
			parseOnly:     false,
			buildOnly:     false,
		},
		{
			name:          "sshd_auth_publickey",
			rtxCommand:    `sshd auth method publickey`,
			terraformJSON: `{"sshd":{"auth_method":"publickey"}}`,
			parseOnly:     false,
			buildOnly:     false,
		},
		{
			name:          "sshd_host_key_generate",
			rtxCommand:    `sshd host key generate`,
			terraformJSON: `{}`,
			parseOnly:     false,
			buildOnly:     true,
		},
		{
			name:          "sftpd_host_single",
			rtxCommand:    `sftpd host lan1`,
			terraformJSON: `{"sftpd":{"hosts":["lan1"]}}`,
			parseOnly:     false,
			buildOnly:     false,
		},
		{
			name:          "sftpd_host_multiple",
			rtxCommand:    `sftpd host lan1 lan2`,
			terraformJSON: `{"sftpd":{"hosts":["lan1","lan2"]}}`,
			parseOnly:     false,
			buildOnly:     false,
		},
		{
			name:          "delete_httpd_host",
			rtxCommand:    `no httpd host`,
			terraformJSON: `{}`,
			parseOnly:     false,
			buildOnly:     true,
		},
		{
			name:          "delete_sshd_service",
			rtxCommand:    `no sshd service`,
			terraformJSON: `{}`,
			parseOnly:     false,
			buildOnly:     true,
		},
		{
			name:          "delete_sshd_host",
			rtxCommand:    `no sshd host`,
			terraformJSON: `{}`,
			parseOnly:     false,
			buildOnly:     true,
		},
		{
			name:          "delete_sftpd_host",
			rtxCommand:    `no sftpd host`,
			terraformJSON: `{}`,
			parseOnly:     false,
			buildOnly:     true,
		},
	}

	for _, tc := range testCases {
		t.Run(tc.name, func(t *testing.T) {
			// Validate RTX command is not empty
			if strings.TrimSpace(tc.rtxCommand) == "" {
				t.Errorf("RTX command should not be empty")
				return
			}

			// build_only tests (like delete commands) may have empty terraform values
			// because they represent commands that don't have a direct terraform mapping
			if tc.buildOnly {
				t.Logf("Direction: Terraform -> RTX only (build)")
				t.Logf("RTX: %s", tc.rtxCommand)
				t.Logf("Terraform: %s (build_only, may be empty)", tc.terraformJSON)
				return
			}

			// Validate terraform JSON is present and valid for non-build_only tests
			if tc.terraformJSON == "null" || tc.terraformJSON == "" {
				t.Errorf("Terraform value is missing for RTX command: %s", tc.rtxCommand)
				return
			}

			var tfValue interface{}
			if err := json.Unmarshal([]byte(tc.terraformJSON), &tfValue); err != nil {
				t.Errorf("Invalid terraform JSON: %v\nJSON: %s", err, tc.terraformJSON)
				return
			}

			// Log the mapping for visibility
			t.Logf("RTX: %s", tc.rtxCommand)
			t.Logf("Terraform: %s", tc.terraformJSON)

			// Validate that terraform value contains expected fields (only for non-build_only tests)
			if tfMap, ok := tfValue.(map[string]interface{}); ok {
				if len(tfMap) == 0 {
					t.Errorf("Terraform value is empty map for RTX command: %s", tc.rtxCommand)
					return
				}
			}

			// Check mapping direction
			if tc.parseOnly {
				t.Logf("Direction: RTX -> Terraform only (parse)")
			} else {
				t.Logf("Direction: Bidirectional (parse and build)")
			}
		})
	}
}

// TestSpecServiceConfigModelCompatibility tests router model compatibility
func TestSpecServiceConfigModelCompatibility(t *testing.T) {
	// All known RTX router models
	allModels := []string{
		"vRX",
		"RTX5000",
		"RTX3510",
		"RTX3500",
		"RTX1300",
		"RTX1220",
		"RTX1210",
		"RTX840",
		"RTX830",
		"RTX810",
		"NVR700W",
		"NVR510",
		"NVR500",
	}

	// Models that support this command according to the spec
	supportedModels := []string{
		"vRX",
		"RTX5000",
		"RTX3510",
		"RTX3500",
		"RTX1300",
		"RTX1220",
		"RTX1210",
		"RTX840",
		"RTX830",
	}

	// Build a map for quick lookup
	supportedMap := make(map[string]bool)
	for _, m := range supportedModels {
		supportedMap[m] = true
	}

	// Find unsupported models
	var unsupportedModels []string
	for _, m := range allModels {
		if !supportedMap[m] {
			unsupportedModels = append(unsupportedModels, m)
		}
	}

	t.Logf("Command: service_config")
	t.Logf("Supported models (%d): %v", len(supportedModels), supportedModels)
	t.Logf("Unsupported models (%d): %v", len(unsupportedModels), unsupportedModels)

	// Test: Verify supported models are recognized
	for _, model := range supportedModels {
		t.Run("supported_"+model, func(t *testing.T) {
			if !IsModelSupported("service_config", model) {
				t.Errorf("Model %s should be supported for service_config", model)
			}
		})
	}

	// Test: Verify unsupported models are rejected
	for _, model := range unsupportedModels {
		t.Run("unsupported_"+model, func(t *testing.T) {
			if IsModelSupported("service_config", model) {
				t.Errorf("Model %s should NOT be supported for service_config", model)
			}
		})
	}
}
