// Code generated by specgen. DO NOT EDIT.
// Source: specs/ip_interface_dhcp_service.yaml
// Command: ip_interface_dhcp_service
// Reference: 12.1.15

package parsers

import (
	"encoding/json"
	"strings"
	"testing"
)

// TestSpecIPInterfaceDhcpServiceRTXSyntax validates that RTX commands in spec are well-formed
func TestSpecIPInterfaceDhcpServiceRTXSyntax(t *testing.T) {
	// This test validates that all RTX command strings in the spec are well-formed
	// and follow expected patterns

	testCases := []struct {
		name       string
		rtxCommand string
		parseOnly  bool
		buildOnly  bool
	}{
		{
			name:       "interface_dhcp_server_lan1",
			rtxCommand: `ip lan1 dhcp service server`,
			parseOnly:  false,
			buildOnly:  false,
		},
		{
			name:       "interface_dhcp_server_lan2",
			rtxCommand: `ip lan2 dhcp service server`,
			parseOnly:  false,
			buildOnly:  false,
		},
		{
			name:       "interface_dhcp_server_bridge1",
			rtxCommand: `ip bridge1 dhcp service server`,
			parseOnly:  false,
			buildOnly:  false,
		},
		{
			name:       "interface_dhcp_relay_single_server",
			rtxCommand: `ip lan2 dhcp service relay 192.168.1.100`,
			parseOnly:  false,
			buildOnly:  false,
		},
		{
			name:       "interface_dhcp_relay_multiple_servers",
			rtxCommand: `ip lan2 dhcp service relay 192.168.1.100 192.168.1.101`,
			parseOnly:  false,
			buildOnly:  false,
		},
		{
			name:       "interface_dhcp_relay_four_servers",
			rtxCommand: `ip lan3 dhcp service relay 10.0.0.1 10.0.0.2 10.0.0.3 10.0.0.4`,
			parseOnly:  false,
			buildOnly:  false,
		},
		{
			name:       "interface_dhcp_off",
			rtxCommand: `ip lan1 dhcp service off`,
			parseOnly:  false,
			buildOnly:  false,
		},
		{
			name:       "delete_interface_dhcp_service",
			rtxCommand: `no ip lan1 dhcp service`,
			parseOnly:  false,
			buildOnly:  true,
		},
		{
			name:       "delete_interface_dhcp_service_lan2",
			rtxCommand: `no ip lan2 dhcp service`,
			parseOnly:  false,
			buildOnly:  true,
		},
	}

	for _, tc := range testCases {
		t.Run(tc.name, func(t *testing.T) {
			// Validate RTX command is not empty
			if strings.TrimSpace(tc.rtxCommand) == "" {
				t.Errorf("RTX command should not be empty")
			}

			// Validate command doesn't have trailing/leading whitespace issues
			trimmed := strings.TrimSpace(tc.rtxCommand)
			if tc.rtxCommand != trimmed && !strings.Contains(tc.rtxCommand, "\n") {
				t.Errorf("RTX command has unexpected whitespace: %q", tc.rtxCommand)
			}

			// Log the command for visibility
			if !tc.buildOnly {
				t.Logf("Parse test: %s", tc.rtxCommand)
			}
			if !tc.parseOnly {
				t.Logf("Build test: %s", tc.rtxCommand)
			}
		})
	}
}

// TestSpecIPInterfaceDhcpServiceSyntaxCoverage documents the syntax patterns covered by this spec
func TestSpecIPInterfaceDhcpServiceSyntaxCoverage(t *testing.T) {
	// This test documents all syntax patterns defined in the spec
	// Each pattern should have corresponding implementation in the parser

	syntaxPatterns := []struct {
		name        string
		rtxCommand  string
		parseOnly   bool
		buildOnly   bool
		description string
	}{
		{
			name:        "interface_dhcp_server_lan1",
			rtxCommand:  `ip lan1 dhcp service server`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "interface_dhcp_server_lan2",
			rtxCommand:  `ip lan2 dhcp service server`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "interface_dhcp_server_bridge1",
			rtxCommand:  `ip bridge1 dhcp service server`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "interface_dhcp_relay_single_server",
			rtxCommand:  `ip lan2 dhcp service relay 192.168.1.100`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "interface_dhcp_relay_multiple_servers",
			rtxCommand:  `ip lan2 dhcp service relay 192.168.1.100 192.168.1.101`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "interface_dhcp_relay_four_servers",
			rtxCommand:  `ip lan3 dhcp service relay 10.0.0.1 10.0.0.2 10.0.0.3 10.0.0.4`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "interface_dhcp_off",
			rtxCommand:  `ip lan1 dhcp service off`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "delete_interface_dhcp_service",
			rtxCommand:  `no ip lan1 dhcp service`,
			parseOnly:   false,
			buildOnly:   true,
			description: "",
		},
		{
			name:        "delete_interface_dhcp_service_lan2",
			rtxCommand:  `no ip lan2 dhcp service`,
			parseOnly:   false,
			buildOnly:   true,
			description: "",
		},
	}

	t.Logf("Total syntax patterns: %d", len(syntaxPatterns))

	parseOnlyCount := 0
	buildOnlyCount := 0
	bidirectionalCount := 0

	for _, p := range syntaxPatterns {
		if p.parseOnly {
			parseOnlyCount++
		} else if p.buildOnly {
			buildOnlyCount++
		} else {
			bidirectionalCount++
		}
	}

	t.Logf("Parse-only patterns: %d", parseOnlyCount)
	t.Logf("Build-only patterns: %d", buildOnlyCount)
	t.Logf("Bidirectional patterns: %d", bidirectionalCount)
}

// TestSpecIPInterfaceDhcpServiceBoundaryCoverage documents the boundary values covered by this spec
func TestSpecIPInterfaceDhcpServiceBoundaryCoverage(t *testing.T) {
	// This test documents all boundary value tests defined in the spec
	boundaryParams := []string{
		"interface",
		"relay_server_count",
		"type",
	}

	t.Logf("Parameters with boundary tests: %d", len(boundaryParams))
	for _, param := range boundaryParams {
		t.Logf("  - %s", param)
	}
}

// TestSpecIPInterfaceDhcpServiceRTXTerraformMapping validates RTX command to Terraform value mappings
func TestSpecIPInterfaceDhcpServiceRTXTerraformMapping(t *testing.T) {
	// This test validates that each RTX command has a corresponding expected Terraform value
	// and vice versa. It ensures the spec file correctly documents the bidirectional mapping.

	testCases := []struct {
		name          string
		rtxCommand    string
		terraformJSON string
		parseOnly     bool
		buildOnly     bool
	}{
		{
			name:          "interface_dhcp_server_lan1",
			rtxCommand:    `ip lan1 dhcp service server`,
			terraformJSON: `{"interface":"lan1","service_type":"server"}`,
			parseOnly:     false,
			buildOnly:     false,
		},
		{
			name:          "interface_dhcp_server_lan2",
			rtxCommand:    `ip lan2 dhcp service server`,
			terraformJSON: `{"interface":"lan2","service_type":"server"}`,
			parseOnly:     false,
			buildOnly:     false,
		},
		{
			name:          "interface_dhcp_server_bridge1",
			rtxCommand:    `ip bridge1 dhcp service server`,
			terraformJSON: `{"interface":"bridge1","service_type":"server"}`,
			parseOnly:     false,
			buildOnly:     false,
		},
		{
			name:          "interface_dhcp_relay_single_server",
			rtxCommand:    `ip lan2 dhcp service relay 192.168.1.100`,
			terraformJSON: `{"interface":"lan2","relay_servers":["192.168.1.100"],"service_type":"relay"}`,
			parseOnly:     false,
			buildOnly:     false,
		},
		{
			name:          "interface_dhcp_relay_multiple_servers",
			rtxCommand:    `ip lan2 dhcp service relay 192.168.1.100 192.168.1.101`,
			terraformJSON: `{"interface":"lan2","relay_servers":["192.168.1.100","192.168.1.101"],"service_type":"relay"}`,
			parseOnly:     false,
			buildOnly:     false,
		},
		{
			name:          "interface_dhcp_relay_four_servers",
			rtxCommand:    `ip lan3 dhcp service relay 10.0.0.1 10.0.0.2 10.0.0.3 10.0.0.4`,
			terraformJSON: `{"interface":"lan3","relay_servers":["10.0.0.1","10.0.0.2","10.0.0.3","10.0.0.4"],"service_type":"relay"}`,
			parseOnly:     false,
			buildOnly:     false,
		},
		{
			name:          "interface_dhcp_off",
			rtxCommand:    `ip lan1 dhcp service off`,
			terraformJSON: `{"interface":"lan1","service_type":"off"}`,
			parseOnly:     false,
			buildOnly:     false,
		},
		{
			name:          "delete_interface_dhcp_service",
			rtxCommand:    `no ip lan1 dhcp service`,
			terraformJSON: `{"interface":"lan1"}`,
			parseOnly:     false,
			buildOnly:     true,
		},
		{
			name:          "delete_interface_dhcp_service_lan2",
			rtxCommand:    `no ip lan2 dhcp service`,
			terraformJSON: `{"interface":"lan2"}`,
			parseOnly:     false,
			buildOnly:     true,
		},
	}

	for _, tc := range testCases {
		t.Run(tc.name, func(t *testing.T) {
			// Validate RTX command is not empty
			if strings.TrimSpace(tc.rtxCommand) == "" {
				t.Errorf("RTX command should not be empty")
				return
			}

			// build_only tests (like delete commands) may have empty terraform values
			// because they represent commands that don't have a direct terraform mapping
			if tc.buildOnly {
				t.Logf("Direction: Terraform -> RTX only (build)")
				t.Logf("RTX: %s", tc.rtxCommand)
				t.Logf("Terraform: %s (build_only, may be empty)", tc.terraformJSON)
				return
			}

			// Validate terraform JSON is present and valid for non-build_only tests
			if tc.terraformJSON == "null" || tc.terraformJSON == "" {
				t.Errorf("Terraform value is missing for RTX command: %s", tc.rtxCommand)
				return
			}

			var tfValue interface{}
			if err := json.Unmarshal([]byte(tc.terraformJSON), &tfValue); err != nil {
				t.Errorf("Invalid terraform JSON: %v\nJSON: %s", err, tc.terraformJSON)
				return
			}

			// Log the mapping for visibility
			t.Logf("RTX: %s", tc.rtxCommand)
			t.Logf("Terraform: %s", tc.terraformJSON)

			// Validate that terraform value contains expected fields (only for non-build_only tests)
			if tfMap, ok := tfValue.(map[string]interface{}); ok {
				if len(tfMap) == 0 {
					t.Errorf("Terraform value is empty map for RTX command: %s", tc.rtxCommand)
					return
				}
			}

			// Check mapping direction
			if tc.parseOnly {
				t.Logf("Direction: RTX -> Terraform only (parse)")
			} else {
				t.Logf("Direction: Bidirectional (parse and build)")
			}
		})
	}
}

// TestSpecIPInterfaceDhcpServiceModelCompatibility tests router model compatibility
func TestSpecIPInterfaceDhcpServiceModelCompatibility(t *testing.T) {
	// All known RTX router models
	allModels := []string{
		"vRX",
		"RTX5000",
		"RTX3510",
		"RTX3500",
		"RTX1300",
		"RTX1220",
		"RTX1210",
		"RTX840",
		"RTX830",
		"RTX810",
		"NVR700W",
		"NVR510",
		"NVR500",
	}

	// Models that support this command according to the spec
	supportedModels := []string{
		"vRX",
		"RTX5000",
		"RTX3510",
		"RTX3500",
		"RTX1300",
		"RTX1220",
		"RTX1210",
		"RTX840",
		"RTX830",
	}

	// Build a map for quick lookup
	supportedMap := make(map[string]bool)
	for _, m := range supportedModels {
		supportedMap[m] = true
	}

	// Find unsupported models
	var unsupportedModels []string
	for _, m := range allModels {
		if !supportedMap[m] {
			unsupportedModels = append(unsupportedModels, m)
		}
	}

	t.Logf("Command: ip_interface_dhcp_service")
	t.Logf("Supported models (%d): %v", len(supportedModels), supportedModels)
	t.Logf("Unsupported models (%d): %v", len(unsupportedModels), unsupportedModels)

	// Test: Verify supported models are recognized
	for _, model := range supportedModels {
		t.Run("supported_"+model, func(t *testing.T) {
			if !IsModelSupported("ip_interface_dhcp_service", model) {
				t.Errorf("Model %s should be supported for ip_interface_dhcp_service", model)
			}
		})
	}

	// Test: Verify unsupported models are rejected
	for _, model := range unsupportedModels {
		t.Run("unsupported_"+model, func(t *testing.T) {
			if IsModelSupported("ip_interface_dhcp_service", model) {
				t.Errorf("Model %s should NOT be supported for ip_interface_dhcp_service", model)
			}
		})
	}
}
