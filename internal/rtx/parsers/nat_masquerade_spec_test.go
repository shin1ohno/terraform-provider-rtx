// Code generated by specgen. DO NOT EDIT.
// Source: specs/nat_masquerade.yaml
// Command: nat_masquerade
// Reference:

package parsers

import (
	"testing"
)

// TestSpecNatMasqueradeSyntaxCoverage documents the syntax patterns covered by this spec
func TestSpecNatMasqueradeSyntaxCoverage(t *testing.T) {
	// This test documents all syntax patterns defined in the spec
	// Each pattern should have corresponding implementation in the parser

	syntaxPatterns := []struct {
		name        string
		rtxCommand  string
		parseOnly   bool
		buildOnly   bool
		description string
	}{
		{
			name:        "nat_type_masquerade",
			rtxCommand:  `nat descriptor type 1 masquerade`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "nat_type_masquerade_100",
			rtxCommand:  `nat descriptor type 100 masquerade`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "nat_address_outer_ipcp",
			rtxCommand:  `nat descriptor address outer 1 ipcp`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "nat_address_outer_ip",
			rtxCommand:  `nat descriptor address outer 1 203.0.113.1`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "nat_address_outer_primary",
			rtxCommand:  `nat descriptor address outer 1 primary`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "nat_address_inner_network",
			rtxCommand:  `nat descriptor address inner 1 192.168.1.1-192.168.1.254`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "nat_address_inner_auto",
			rtxCommand:  `nat descriptor address inner 1 auto`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "masquerade_static_tcp",
			rtxCommand:  `nat descriptor masquerade static 1 1 192.168.1.100 tcp 80`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "masquerade_static_tcp_https",
			rtxCommand:  `nat descriptor masquerade static 1 2 192.168.1.100 tcp 443`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "masquerade_static_udp",
			rtxCommand:  `nat descriptor masquerade static 1 3 192.168.1.200 udp 53`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "masquerade_static_port_mapping",
			rtxCommand:  `nat descriptor masquerade static 1 4 192.168.1.100 tcp 8080=80`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "masquerade_static_outer_inner",
			rtxCommand:  `nat descriptor masquerade static 1 5 203.0.113.1:22=192.168.1.10:22`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "masquerade_static_protocol_only",
			rtxCommand:  `nat descriptor masquerade static 1 6 192.168.1.100 icmp`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "interface_nat_descriptor",
			rtxCommand:  `ip lan2 nat descriptor 1`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "interface_nat_descriptor_pp",
			rtxCommand:  `ip pp nat descriptor 1`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "delete_nat_masquerade",
			rtxCommand:  `no nat descriptor type 1`,
			parseOnly:   false,
			buildOnly:   true,
			description: "",
		},
		{
			name:        "delete_masquerade_static",
			rtxCommand:  `no nat descriptor masquerade static 1 1`,
			parseOnly:   false,
			buildOnly:   true,
			description: "",
		},
	}

	t.Logf("Total syntax patterns: %d", len(syntaxPatterns))

	parseOnlyCount := 0
	buildOnlyCount := 0
	bidirectionalCount := 0

	for _, p := range syntaxPatterns {
		if p.parseOnly {
			parseOnlyCount++
		} else if p.buildOnly {
			buildOnlyCount++
		} else {
			bidirectionalCount++
		}
	}

	t.Logf("Parse-only patterns: %d", parseOnlyCount)
	t.Logf("Build-only patterns: %d", buildOnlyCount)
	t.Logf("Bidirectional patterns: %d", bidirectionalCount)
}

// TestSpecNatMasqueradeBoundaryCoverage documents the boundary values covered by this spec
func TestSpecNatMasqueradeBoundaryCoverage(t *testing.T) {
	// This test documents all boundary value tests defined in the spec
	boundaryParams := []string{
		"descriptor_id",
		"entry_number",
		"port",
		"protocol",
	}

	t.Logf("Parameters with boundary tests: %d", len(boundaryParams))
	for _, param := range boundaryParams {
		t.Logf("  - %s", param)
	}
}

// TestSpecNatMasqueradeModelCompatibility tests router model compatibility
func TestSpecNatMasqueradeModelCompatibility(t *testing.T) {
	// All known RTX router models
	allModels := []string{
		"vRX",
		"RTX5000",
		"RTX3510",
		"RTX3500",
		"RTX1300",
		"RTX1220",
		"RTX1210",
		"RTX840",
		"RTX830",
		"RTX810",
		"NVR700W",
		"NVR510",
		"NVR500",
	}

	// Models that support this command according to the spec
	supportedModels := []string{
		"vRX",
		"RTX5000",
		"RTX3510",
		"RTX3500",
		"RTX1300",
		"RTX1220",
		"RTX1210",
		"RTX840",
		"RTX830",
	}

	// Build a map for quick lookup
	supportedMap := make(map[string]bool)
	for _, m := range supportedModels {
		supportedMap[m] = true
	}

	// Find unsupported models
	var unsupportedModels []string
	for _, m := range allModels {
		if !supportedMap[m] {
			unsupportedModels = append(unsupportedModels, m)
		}
	}

	t.Logf("Command: nat_masquerade")
	t.Logf("Supported models (%d): %v", len(supportedModels), supportedModels)
	t.Logf("Unsupported models (%d): %v", len(unsupportedModels), unsupportedModels)

	// Test: Verify supported models are recognized
	for _, model := range supportedModels {
		t.Run("supported_"+model, func(t *testing.T) {
			if !IsModelSupported("nat_masquerade", model) {
				t.Errorf("Model %s should be supported for nat_masquerade", model)
			}
		})
	}

	// Test: Verify unsupported models are rejected
	for _, model := range unsupportedModels {
		t.Run("unsupported_"+model, func(t *testing.T) {
			if IsModelSupported("nat_masquerade", model) {
				t.Errorf("Model %s should NOT be supported for nat_masquerade", model)
			}
		})
	}
}
