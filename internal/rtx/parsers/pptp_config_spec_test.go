// Code generated by specgen. DO NOT EDIT.
// Source: specs/pptp_config.yaml
// Command: pptp_config
// Reference:

package parsers

import (
	"testing"
)

// TestSpecPptpConfigSyntaxCoverage documents the syntax patterns covered by this spec
func TestSpecPptpConfigSyntaxCoverage(t *testing.T) {
	// This test documents all syntax patterns defined in the spec
	// Each pattern should have corresponding implementation in the parser

	syntaxPatterns := []struct {
		name        string
		rtxCommand  string
		parseOnly   bool
		buildOnly   bool
		description string
	}{
		{
			name:        "pptp_service_on",
			rtxCommand:  `pptp service on`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "pptp_service_off",
			rtxCommand:  `pptp service off`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "pptp_disconnect_time",
			rtxCommand:  `pptp tunnel disconnect time 300`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "pptp_disconnect_time_600",
			rtxCommand:  `pptp tunnel disconnect time 600`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "pptp_keepalive_on",
			rtxCommand:  `pptp keepalive use on`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "pptp_keepalive_off",
			rtxCommand:  `pptp keepalive use off`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "pptp_auth_accept_mschapv2",
			rtxCommand:  `pp auth accept mschapv2`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "pptp_auth_accept_chap",
			rtxCommand:  `pp auth accept chap`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "pptp_auth_myname",
			rtxCommand:  `pp auth myname vpnuser vpnpassword`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "ppp_ccp_mppe128",
			rtxCommand:  `ppp ccp type mppe-128`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "ppp_ccp_mppe128_require",
			rtxCommand:  `ppp ccp type mppe-128 require`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "ppp_ccp_mppe56",
			rtxCommand:  `ppp ccp type mppe-56`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "ppp_ccp_mppe40",
			rtxCommand:  `ppp ccp type mppe-40`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "pptp_ip_pool",
			rtxCommand:  `ip pp remote address pool 192.168.10.100-192.168.10.200`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "pptp_ip_pool_small",
			rtxCommand:  `ip pp remote address pool 10.0.0.10-10.0.0.20`,
			parseOnly:   false,
			buildOnly:   false,
			description: "",
		},
		{
			name:        "delete_pptp_service",
			rtxCommand:  `pptp service off`,
			parseOnly:   false,
			buildOnly:   true,
			description: "",
		},
		{
			name:        "delete_pptp_disconnect_time",
			rtxCommand:  `no pptp tunnel disconnect time`,
			parseOnly:   false,
			buildOnly:   true,
			description: "",
		},
		{
			name:        "delete_pptp_keepalive",
			rtxCommand:  `pptp keepalive use off`,
			parseOnly:   false,
			buildOnly:   true,
			description: "",
		},
	}

	t.Logf("Total syntax patterns: %d", len(syntaxPatterns))

	parseOnlyCount := 0
	buildOnlyCount := 0
	bidirectionalCount := 0

	for _, p := range syntaxPatterns {
		if p.parseOnly {
			parseOnlyCount++
		} else if p.buildOnly {
			buildOnlyCount++
		} else {
			bidirectionalCount++
		}
	}

	t.Logf("Parse-only patterns: %d", parseOnlyCount)
	t.Logf("Build-only patterns: %d", buildOnlyCount)
	t.Logf("Bidirectional patterns: %d", bidirectionalCount)
}

// TestSpecPptpConfigBoundaryCoverage documents the boundary values covered by this spec
func TestSpecPptpConfigBoundaryCoverage(t *testing.T) {
	// This test documents all boundary value tests defined in the spec
	boundaryParams := []string{
		"disconnect_time",
		"max_connections",
		"mppe_bits",
	}

	t.Logf("Parameters with boundary tests: %d", len(boundaryParams))
	for _, param := range boundaryParams {
		t.Logf("  - %s", param)
	}
}

// TestSpecPptpConfigModelCompatibility tests router model compatibility
func TestSpecPptpConfigModelCompatibility(t *testing.T) {
	// All known RTX router models
	allModels := []string{
		"vRX",
		"RTX5000",
		"RTX3510",
		"RTX3500",
		"RTX1300",
		"RTX1220",
		"RTX1210",
		"RTX840",
		"RTX830",
		"RTX810",
		"NVR700W",
		"NVR510",
		"NVR500",
	}

	// Models that support this command according to the spec
	supportedModels := []string{
		"vRX",
		"RTX5000",
		"RTX3510",
		"RTX3500",
		"RTX1300",
		"RTX1220",
		"RTX1210",
		"RTX840",
		"RTX830",
	}

	// Build a map for quick lookup
	supportedMap := make(map[string]bool)
	for _, m := range supportedModels {
		supportedMap[m] = true
	}

	// Find unsupported models
	var unsupportedModels []string
	for _, m := range allModels {
		if !supportedMap[m] {
			unsupportedModels = append(unsupportedModels, m)
		}
	}

	t.Logf("Command: pptp_config")
	t.Logf("Supported models (%d): %v", len(supportedModels), supportedModels)
	t.Logf("Unsupported models (%d): %v", len(unsupportedModels), unsupportedModels)

	// Test: Verify supported models are recognized
	for _, model := range supportedModels {
		t.Run("supported_"+model, func(t *testing.T) {
			if !IsModelSupported("pptp_config", model) {
				t.Errorf("Model %s should be supported for pptp_config", model)
			}
		})
	}

	// Test: Verify unsupported models are rejected
	for _, model := range unsupportedModels {
		t.Run("unsupported_"+model, func(t *testing.T) {
			if IsModelSupported("pptp_config", model) {
				t.Errorf("Model %s should NOT be supported for pptp_config", model)
			}
		})
	}
}
